/** Preset management: load, save, and transfer presets to/from the device. */
export class PresetManager {
  async loadFromDevice(_index: number): Promise<void> {
    // TODO: implement preset loading over serial
  }

  async saveToDevice(_index: number): Promise<void> {
    // TODO: implement preset saving over serial
  }
}
